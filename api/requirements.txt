# Vercel Python function dependencies (web/api/).
fastapi>=0.115,<1
pydantic>=2.10,<3

# Question generation.
google-genai
python-dotenv

# Durable state + retrieval: Supabase Postgres (chunks, attempts, answers,
# student_state, attempt_contexts) -- replaces local SQLite/Chroma/in-memory
# state. No chromadb/sentence-transformers needed: retrieval.py filters
# chunks by (topic, language) only -- see its module docstring.
psycopg[binary]

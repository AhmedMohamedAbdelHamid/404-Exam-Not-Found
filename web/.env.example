# URL of the FastAPI backend, called directly from the browser (no trailing slash).
# Local: http://localhost:8000  |  Deployed: your API project's Vercel URL.
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000
